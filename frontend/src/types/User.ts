export interface User {
  _id: string; // or number, depending on your database schema
  username: string;
  fundAmount: number; // Adjust according to your data structure
  // Add other relevant fields as needed
}
